# 片音分析概要

片音分析是从唱音分析进入音元分析的中间环节。唱音分析说明音节中的音调和音质如何相对独立又并行联结；片音分析进一步根据音高和音质的特征及其变化，把连续的语音分析成片音序列；音元分析则确定表示这些片音的音元，从而把音节表示成音元序列。

本概要先说明片音分析采用的基本模型和术语边界。它不替代后文的具体分析，也不把尚待实验的问题预先当作已经证明的语音事实。

## 片音不是平顶等长音段

片音不是按照固定时长的时间窗从语流中等距截取的波形块，也不是通过平顶抽样取得的内部音高和音质始终不变的音段。片音的时长可以不同，它的内部及边界处可以存在连续的音高变化和音质过渡。

一个片音具有单一音高特征和单一音质特征，是指这个不等长的特征段可以被发出并被听知为一个确定的质调类别，不是指它在整个时段内的基频、共振峰和其它声学参数保持恒定。这里的“单一特征”是分析和听知所得的类别特征，不是平顶信号的物理性质。

因此，片音的边界原则上应根据音高或音质的区别特征及其变化来确定，不能仅根据统一的时间长度来确定。相邻片音之间可以连续过渡，分析得到的边界也不等于自然语流中必然存在可以直接剪开的绝对物理边界。

## 能单发和能听知

片音是能单发、能听知的最小短音。这里的“能单发”，是指发音者能够有意产生近似该片音质调特征的短音；“能听知”，是指听者能够把这个短音辨认为相应的质调类别。

能单发、能听知，不等于片音必须是等长、平顶、边界绝对清楚的音段，也不等于从自然语流中截取片音后可以不经任何处理直接重新拼成音节。在游离状态下发出的片音是相应质调类别的一种实现；在音节中实现的片音还会受到前后片音、音节位置、相对音长和协同发音等条件的制约。

## 样本、片音和音元

本项目所说的样本，不是数字信号处理中按固定采样率取得的瞬时幅值，也不是按等长时间窗截取的原始波形。它是从连续语音中观察或抽取的、能够代表某种音高和音质状态的特征样本。

片音是表示这种特征样本的特征段。特征样本可以表现为片音中的特征点、主要状态或具有一定延续时间的特征区域；片音则保留实现这个特征所需的音段及其不等长时限。把片音称为“段”，不表示它的内部完全静止；把样本称为“点”，也不表示发音和听知可以脱离必要的持续时间。

音元不是片音本身，而是表示片音的变元。一个音元可以根据分布环境取用不同的条件片音；片音是音元在特定环境中所取的音值。Yinyuan ID（例如 `M01`、`N01`）只是这个抽象音元的唯一编号，不是片音的录音、音标或实际音值。

这三个层次可以简记为：

```text
连续语音中的特征样本
  -> 用不等长特征段表示为片音
  -> 将同类或按规则归并的片音表示为音元
  -> 用 Yinyuan ID 唯一标识音元
```

## 片音序列的拼合

按照片音序列发音，不应理解为把若干平顶录音片段机械地首尾相接。片音提供的是依次实现的质调特征目标。实际拼合时，各目标之间需要形成连续过渡；如果用程序模拟或合成，可能需要进行音高曲线和音质参数的插值、时长配置、边界平滑以及协同发音处理。

可以把这个过程概括为：

```text
音元序列
  -> 根据分布环境为音元取值
  -> 得到片音及其质调特征目标序列
  -> 配置相对时长并在目标之间插值和过渡
  -> 实现连续音节
```

因此，片音分析提供的不是一组可以机械拼接的静态声块，而是一组沿时间排列、可以指导发音、听知和语音实现的质调特征段。

## 当前成立范围

片音分析和音元分析目前至少可以作为一种音节表示和汉字输入编码方法：它把音节中相对独立的音调结构和音质结构展开为具有对应关系的序列，再把对应的质调组合表示为音元，使音节能够转换成稳定的 Yinyuan ID 序列。

片音是否是自然语音中普遍存在的最小单位、听者能否稳定辨认全部片音类别、片音的最短听知时限和分类边界在哪里，以及根据片音目标插值能否充分重建自然音节，仍需要语音产生、听辨和合成实验验证。在取得这些证据以前，应把这些内容视为可检验的分析假说，不把输入编码已经可用直接当作语音学理论已经得到证明。

后续的[音元分析法](音元分析法.md)在这个片音模型的基础上，说明怎样确定表示片音的音元，并把现代通用汉语音节分析成音元序列。
